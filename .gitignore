.agents

G.O.D

# --- epoch-calibration pilot: never commit dataset, base models, checkpoints, eval outputs ---
# global safety nets
*.safetensors
*.png
*.jpg
*.jpeg
**/__pycache__/

# pilot working dirs (regenerated by the scripts on the GPU box)
calibration/base_model/
calibration/checkpoints/
calibration/eval_out/
calibration/out/

# dataset (images + captions) — keep ONLY the manifest, never the data itself
calibration/data/**
!calibration/data/**/
!calibration/data/**/split_manifest.json
